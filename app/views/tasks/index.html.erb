<div class="container">
  <div class="wrapper col-md-10 col-md-offset-1 col-sm-10 col-sm-offset-1">
<p id="notice"><%= notice %></p>

<h1>タスク一覧</h1>
<%= link_to '新しいタスクの作成', new_task_path, class: 'btn btn-default' %>
<hr>

<div class="row">
  <div class="col-md-6 col-sm-12">
<h3>自分が担当するタスク</h3>
<% @tasks.where(charge_id: current_user.id).each do |task| %> <!--viewでもwhere句で条件指定できる-->
<table class="taskindex">
    <tr>
      <td class="tasktd"><strong>○　標題</strong></td>
      <td class="tasktd"><h4><strong><%= link_to task_path(task) do %><%= task.title %><% end %></strong></h4></td>
    </tr>
    <tr>
      <td class="tasktd">○　依頼者</td>
      <td class="tasktd"><%= task.user.name %></td>
    </tr>
    <tr>
      <td class="tasktd">○　内容</td>
      <td class="tasktd"><%= task.content %></td>
    </tr>
    <tr>
      <td class="tasktd">○　期限</td>
      <td class="tasktd"><%= task.deadline.strftime("%Y年%m月%d日 %H時%M分") %></td>
    </tr>
    <tr>
      <td class="tasktd"><strong>○　担当者</strong></td>
      <td class="tasktd"><strong><%= task.charge.name %></strong></td>
    </tr>
    <tr>
      <td class="tasktd">○　進捗状況</td>
      <td class="tasktd"><%= task.status %></td>
    </tr>
 </table>

<div style="text-align: center;">
<%= link_to 'タスクの編集', edit_task_path(task), class: 'btn btn-info' %>　　
<%= link_to 'タスクの削除', task_path(task), method: :delete, data: { confirm: '本当に削除してもよろしいですか？' } , class: 'btn btn-danger' %>
</div>
<br>
<br>

<% end %>

</div>

<div class="col-md-6 col-sm-12">
<h3>自分が作成したタスク</h3>
<% @tasks.where(user_id: current_user.id).each do |task| %>
<table class="taskindex">
    <tr>
      <td class="tasktd"><strong>○　標題</strong></td>
      <td class="tasktd"><h4><strong><%= link_to task_path(task) do %><%= task.title %><% end %></strong></h4></td>
    </tr>
    <tr>
      <td class="tasktd"><strong>○　依頼者</strong></td>
      <td class="tasktd"><strong><%= task.user.name %></strong></td>
    </tr>
    <tr>
      <td class="tasktd">○　内容</td>
      <td class="tasktd"><%= task.content %></td>
    </tr>
    <tr>
      <td class="tasktd">○　期限</td>
      <td class="tasktd"><%= task.deadline.strftime("%Y年%m月%d日 %H時%M分") %></td>
    </tr>
    <tr>
      <td class="tasktd">○　担当者</td>
      <td class="tasktd"><%= task.charge.name %></td>
    </tr>
    <tr>
      <td class="tasktd">○　進捗状況</td>
      <td class="tasktd"><%= task.status %></td>
    </tr>
 </table>

<div style="text-align: center;">
<%= link_to 'タスクの編集', edit_task_path(task), class: 'btn btn-info' %>　　
<%= link_to 'タスクの削除', task_path(task), method: :delete, data: { confirm: '本当に削除してもよろしいですか？' } , class: 'btn btn-danger' %>
</div>
<br>
<br>

<% end %>


</div>

</div>
</div>
</div>
